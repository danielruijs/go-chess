services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    restart: unless-stopped
    command: ["-allowed-origins", "https://gochess.dev", "-cookie-domain", ".gochess.dev"]
    ports:
      - "8085:8085"
      - "2115:2115"
