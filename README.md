# Go-chess

The application is currently deployed at https://gochess.dev/

## Running the application

To run both the frontend and backend, use the following commands:

```bash
cd backend && task run
cd frontend && task run
```

## Linting and running tests

To lint and run tests for the frontend and backend, use the following command:

```bash
task ci
```
